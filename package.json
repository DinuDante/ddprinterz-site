{
  "name": "ddprinterz-site",
  "private": true,
  "description": "Static site for dinudante.com (DDPrinterZ). The published site is dependency-free; these packages are build and QA tooling only.",
  "scripts": {
    "build": "node tools/build-images.js && node build.js",
    "build:pages": "node build.js",
    "serve": "node tools/serve.js",
    "check": "node tools/check.js",
    "qa": "node tools/qa.js && node tools/qa-interactions.js && node tools/qa-a11y.js && node tools/qa-perf.js"
  },
  "devDependencies": {
    "axe-core": "^4.10.2",
    "puppeteer-core": "^24.0.0",
    "sharp": "^0.34.0"
  }
}
